# backend/requirements.txt

# FastAPI 和 ASGI 服务器
fastapi>=0.109.2,<0.111.0 # 使用一个明确的、较新的稳定版本范围
uvicorn[standard]>=0.27.1,<0.29.0 # 同样，明确版本范围

# Socket.IO
python-socketio>=5.10.0,<5.12.0

# TQSDK (请根据你的测试选择一个稳定的版本)
tqsdk>=2.6.10 

# SQLAlchemy 和数据库驱动
sqlalchemy[asyncio]>=2.0.25,<2.1.0 # 强制使用 SQLAlchemy 2.0+
asyncpg>=0.28.0,<0.30.0       # PostgreSQL 异步驱动
psycopg2-binary>=2.9.9 # Alembic 或某些同步 ORM 操作可能需要

# 配置管理
python-dotenv>=1.0.0,<1.1.0

# 日志库
loguru>=0.7.2,<0.8.0

# 数据库迁移工具 (如果使用)
alembic>=1.13.1,<1.14.0

# WSGI/ASGI 生产服务器 (用于 Gunicorn + Uvicorn worker)
gunicorn>=21.2.0,<22.1.0

# Pydantic (FastAPI 的核心依赖)
pydantic>=2.5.3,<2.8.0
# pydantic-settings # 如果使用 Pydantic V2 的设置管理功能 (可选)