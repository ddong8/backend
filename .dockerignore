# backend/.dockerignore

# Python 缓存文件和目录
__pycache__/
*.py[cod]
*$py.class

# 虚拟环境目录 (常见的名称)
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE 和编辑器特定的配置文件和目录
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
nbproject/

# Git 特定文件和目录
.git/
.gitignore # 通常 .gitignore 本身也应该被 git 忽略，但 Docker 构建时可以包含

# 日志文件目录 (如果日志通过卷映射到主机，则不需要包含在镜像中)
# logs/ 

# 本地配置文件 (通常不应复制到镜像中，而是通过 Docker Compose 的 env_file 或 environment 注入)
# .env # 这个 .env 文件本身

# 测试相关文件和目录
.pytest_cache/
.coverage
htmlcov/
*.prof
*.lprof
*.cprof
tests/ # 如果测试代码不随应用部署

# 其他通常不需要包含在 Docker 镜像中的文件
*.local
*.orig
*.swp
*~